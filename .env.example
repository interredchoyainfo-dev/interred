# ============================================
# INTER RED — Variables de entorno
# Copiá este archivo como .env y completá los valores
# NUNCA subas .env al repositorio
# ============================================

# Servidor
PORT=3000

# Auth del panel admin (usuario y contraseña del login)
ADMIN_USER=Admin
ADMIN_PASS=Giolezana19

# Token secreto para validar requests frontend → backend
# Generá uno con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_SECRET=reemplaza_esto_con_un_token_largo_y_aleatorio

# MikroTik (fallback si no hay config en DB)
MIKROTIK_HOST=181.209.118.162
MIKROTIK_USER=interred_api
MIKROTIK_PASS=InterRed2026
MIKROTIK_PORT=8729
